import palette from './palette';

const formCheckInput = {
  colors: {
    default: {
      colorDisabled: palette.darkLighter,
      colorDisabledLabel: palette.darkLighter,
    },
  },
  margin: {
    top: '0.3rem',
    left: '0rem',
  },
};

export default formCheckInput;
